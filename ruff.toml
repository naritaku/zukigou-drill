line-length = 120

[lint]
# E/W: pycodestyle, F: pyflakes（未使用 import・未定義名）, B: bugbear（可変デフォルト引数、
# zip の長さ不一致など、レビューで人間が拾っていた型のバグ）
select = ["E", "W", "F", "B"]
# 長い行はプロンプト本文と 404 ページのインライン HTML に集中している。どちらも
# 折り返すと読みにくくなる「文章」なので、行長だけは強制しない。
ignore = ["E501"]
